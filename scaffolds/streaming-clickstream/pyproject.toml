[project]
name = "streaming-clickstream"
version = "0.1.0"
# Spark 4.0 supports Python 3.13 — matches the lab/base workspace.
requires-python = ">=3.13"
dependencies = [
    "pyspark==4.0.0",
    "kafka-python>=2.0,<3",
    "jupyterlab>=4,<5",
    "ipykernel",
    "pandas>=2,<3",
    "pyarrow>=15",
    "pytest>=8,<9",
    "pytest-timeout>=2,<3",
]
